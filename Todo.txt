--SOLVED: sometimes NNoutput[1] is < 0 ... --> accelerate too, but not the same amount...... how/why ? solved: mutation changes activation function

--new map

--multiple cars at once
        --> rewrite A LOT to do it with class car